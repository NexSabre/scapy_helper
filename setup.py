from setuptools import setup, find_packages

setup(
    name="scapy_helper",
    author="NexSabre",
    description="Few functions which make a scapy easy",
    version="0.1",
    url="",
    packages=find_packages(),
    install_requires=["scapy"]
)
